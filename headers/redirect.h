int redirect(char **,int , int *, int , int );      // Handles input with redirection characters
int which_red(char *);                              // Identifies what kind of redirection character a word is , if any
