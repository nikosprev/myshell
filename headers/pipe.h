
#ifndef PIPE_H
#define PIPE_H


int pipe_check(char *);                        // Checks if a word is the pipe character
void pipe_exec(char *, Aliases *,History *);               // Executes a pipe


#endif